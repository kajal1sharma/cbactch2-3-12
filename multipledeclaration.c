#include <stdio.h>

int main()
{
     
    int a,b,c; // multiple variable declaration
    float d; // single variable declaration
    scanf("%d%d%d",&a,&b,&c);
    //&a the address of a 
    printf("the number stored is:: %d  %d  %d",a,b,c);
    return 0;
}
